-   target : E.listItems(E.fetch(Lo.load(L.listFiles('../res/datas/*')), 'players'))
    data :
        current: E.listItems(E.fetch(Lo.load(current.filepath), current.datapath)).data[current.number]
    template : single.j2
    output : "'../res/out/'+E.fetch(current, 'name').data+'.txt'"
