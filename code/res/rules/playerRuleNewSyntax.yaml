-   target : L.listFiles('../res/datas/*.json') >> Lo.load() >> E.fetch('players') >> E.paginate(1)
    data :
        current: current
    template : single.j2
    output : W.text('../res/out/') + E.fetch(current, 'name') + W.text(current.pageNum) + W.text('.txt')
