-   target : L.listFiles('../res/datas/*') >> Lo.load() >> E.fetch('players') >> E.paginate(1)
    data :
        current: current
    template : single.j2
    output : "'../res/out/'+T.getFilename(E.fetch(current, 'name').filepath)+ str(current.pageNum) +'.txt'"
