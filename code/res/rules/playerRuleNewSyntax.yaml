-   target : L.listFiles('../res/datas/*') >> Lo.load() >> E.fetch('players') >> E.paginate(1)
    data :
        current: current
    template : single.j2
    output : "'../res/out/'+E.fetch(current.item, 'name').data+'.txt'"
