-   target : L.listFiles('../res/datas/*.json') >> Lo.load() >> E.fetch('players') >> MF.map_(DT.toUpper('name')) >> E.paginate(1)
    data :
        current: current
    template : single.j2
    output : "'../res/out/'+T.getFilename(E.fetch(current, 'name').data)+ str(current.pageNum) +'.txt'"
